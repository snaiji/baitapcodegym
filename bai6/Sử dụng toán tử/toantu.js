let inputWidth = prompt("a");
let inputHeight = prompt("b");
let width = parseInt(inputWidth);
let height = parseInt(inputHeight);
let area = width * height;
document.write(" Tích hai số " + width + " và " + height + " bằng " + area);